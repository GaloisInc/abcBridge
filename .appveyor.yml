platform:
  - x64

build: off

install:
  - choco install -y ghc --version 8.2.1
  - refreshenv
  - set PATH=%APPDATA%\cabal\bin;C:\msys64\usr\bin;%PATH%

environment:
  global:
    MSYSTEM: MINGW64

build_script:
  - cabal update
  - cabal install alex
  - cabal install happy
  - cabal install c2hs
  - cabal new-build

test_script:
  - cabal new-test
